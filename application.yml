eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
zipkin:
  baseUrl: http://localhost:9411/
management:
  endpoints:
    web:
      exposure:
        include: "*"

spring:
  data:
    mongodb:
      host: 127.0.0.1
      port: 27017
      authentication-database: admin
      username: root
      password: root
